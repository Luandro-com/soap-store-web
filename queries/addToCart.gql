mutation($input: CartInput!){
  addToCart(input: $input) {
    id
    products {
      id
      quantity
      product {
        id
        name
        price
        image
        slug
      }
    }
  }
}